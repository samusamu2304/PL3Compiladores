let x = 0;
let seguir = true;
while (seguir) {show(x);
 x++;
 if (x>10) then seguir = false;
 };
 let y = "hola";
 for (let x = 0; x<10; x++) {
     show(x);
     show(y);
     for (let y = 0; y<5; y++) {
        show(y);
     };
 };
